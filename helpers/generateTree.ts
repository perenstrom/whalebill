import { ResultNodeOptions } from 'types/types';

export const generateTree = (options: ResultNodeOptions) => {
  // Generate starting options-hash
  // Add to queue
  // While queue is not empty
    // Shift array
    // Generate node
    // Save options-hash -> node-hash
    // Check if node exists
    // Calculate results
    // Save options-hash on current node
    // Add to queue
  // Loop through and replace hash

  return null;
};
